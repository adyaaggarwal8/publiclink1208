<!DOCTYPE html>
<html>
<h1> Multiplier<h1>

<script>
let num = prompt("Enter your no");
num = Number(num);
 if (!isNaN(num)){
    for(let i=1;i<=10;i++){
        document.write(num + "x" + i + "=" + (num*i) + "<br>");}
    }
 else {document.write("invalid no:");}

</script>
<html>
